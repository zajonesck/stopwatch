import { render, fireEvent } from "@testing-library/react";

describe("Test button-stop function", () => {
  test(".onclick button-stop stopwatch stops", async () => {
      ////simulate .onclick button-start
      ////verify button-start works
      ////simulate .onclick button-stop
      ////
      });
    expect(tenths).toStrictEqual({});
  });
});
